<UserControl x:Class="p2_projekt.WPF.BoatForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             xmlns:controllers="clr-namespace:p2_projekt.WPF"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <!--Binding Name er boat.name-->
        <controllers:InfolineController Title="Ejer(e)" Value="{Binding User.Name}" />
        <controllers:InfolineController Title="Navn" Value="{Binding Name,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <controllers:InfolineController Title="Plads" Value="{Binding BoatSpace}"/>
        <controllers:InfolineController Title="ID" Value="{Binding BoatId}" Visibility="Collapsed"/>
        <!--TODO HEYEYEYYE boatID skal medlemmer ikke se mand!-->
        <controllers:InfolineController Title="Reg. nr." Value="{Binding RegistrationNumber}"/>
        <controllers:InfolineController Title="Længde" Value="{Binding Length}"/>
        <controllers:InfolineController Title="Bredde" Value="{Binding Width}"/>
    </StackPanel>
</UserControl>
